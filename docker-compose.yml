services:  
  postgres:
    image: postgres:15
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: keycloak
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./docker/db-init:/docker-entrypoint-initdb.d
    networks:
      - aether-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  neo4j:
    image: neo4j:5.18
    container_name: neo4j
    restart: unless-stopped
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: ${NEO4J_USER}/${NEO4J_PASSWORD}
      NEO4J_PLUGINS: '["apoc","graph-data-science"]'
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    networks:
      - aether-network
    healthcheck:
      test: ["CMD", "cypher-shell", "-u", "${NEO4J_USER}", "-p", "${NEO4J_PASSWORD}", "RETURN 1"]
      interval: 10s
      timeout: 5s
      retries: 10

  # ============================================================================
  # AUTHENTICATION & BACKEND
  # ============================================================================

  keycloak:
    image: quay.io/keycloak/keycloak:24.0.2
    container_name: keycloak
    depends_on:
      postgres:
        condition: service_healthy
    command: 
      - start-dev 
      - --import-realm
      - --hostname=localhost
      - --hostname-port=8080
      - --hostname-strict=false
      - --hostname-strict-https=false
      - --http-enabled=true
    ports:
      - "8080:8080"
    restart: always
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: ${DB_USER}
      KC_DB_PASSWORD: ${DB_PASSWORD}
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KC_HOSTNAME: localhost
      KC_HOSTNAME_PORT: 8080
      KC_HOSTNAME_STRICT: "false"
      KC_HOSTNAME_STRICT_HTTPS: "false"
      KC_HTTP_ENABLED: "true"
    volumes:
      - keycloak_data:/opt/keycloak/data
      - ./keycloak/exports:/opt/keycloak/data/import
    networks:
      - aether-network

  backend:
    build: ./backend/app
    restart: on-failure
    env_file:
      - .env.dev
    environment:
      DB_URL: postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/aether
      KEYCLOAK_URL: http://${KEYCLOAK_HOSTNAME}:8080/realms/Aether
      SWAGGER_TOKEN_URL: http://localhost:8080/realms/Aether/protocol/openid-connect/token
      ENVIRONMENT: ${ENVIRONMENT}
      PYTHONPATH: /app:/app/lib
      REDIS_HOST: redis
      REDIS_PORT: 6379
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - tg_sessions:/app/sessions
      - ${MEDIA_PATH}:/shared/media
      - ./aether_lib:/app/aether_lib:ro
    depends_on:
      postgres:
        condition: service_healthy
      keycloak:
        condition: service_started
      redis:
        condition: service_healthy
      neo4j:
        condition: service_healthy
    ports:
      - "8000:8000"
    networks:
      - aether-network

  # ============================================================================
  # FRONTEND
  # ============================================================================

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: http://localhost:8000/api
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend:/app
      - ./frontend/nginx-dev.conf:/etc/nginx/conf.d/default.conf:ro
      - ./shared/media:/usr/share/nginx/html/app/public/media
    restart: always
    depends_on:
      - backend
    networks:
      - aether-network

  # ============================================================================
  # TELEGRAM SCRAPER (Updated with lib/)
  # ============================================================================

  telegram-worker:
    build:
      context: ./telegram_scraper
    image: telegram-job:latest
    command: rq worker --url redis://redis:6379 telegram-jobs
    restart: unless-stopped
    environment:
      # Telegram API
      TG_API_ID: ${TG_API_ID}
      TG_API_HASH: ${TG_API_HASH}
      # Neo4j
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_USER: ${NEO4J_USER}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
      # Postgres
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      # Job Launcher
      JOB_LAUNCHER_URL: ${JOB_LAUNCHER_URL}
      JOB_SECRET_TOKEN: ${JOB_SECRET_TOKEN}
      # Media
      MEDIA_ROOT: /shared/media
      # Redis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      # Owner ID
      OWNER_ID: ${OWNER_ID:-unknown}
      # Python path for lib/
      PYTHONPATH: /app:/app/lib
    volumes:
      - tg_sessions:/app/sessions
      - ./shared/media:/shared/media
      - ./aether_lib:/app/aether_lib:ro
    deploy:
      replicas: 2
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      keycloak:
        condition: service_started
    networks:
      - aether-network

  # ============================================================================
  # TRANSLATION WORKER (NEW)
  # ============================================================================

  redis:
    image: redis:7-alpine
    container_name: aether-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - aether-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Translation worker (multiple instances for parallelism)
  translation-worker:
    build:
      context: .
      dockerfile: workers/translation_worker/Dockerfile
    depends_on:
      keycloak:
        condition: service_started
      translation-models:
        condition: service_started
    environment:
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - MODELS_PATH=/app/models
      - USE_QUANTIZATION=true
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=${NEO4J_USER}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD}
    volumes:
      # Mount local models directory
      - ./aether_lib:/app/aether_lib:ro
      - translation_models:/app/models/translation
    networks:
      - aether-network
    deploy:
      replicas: 1  # Run 2 worker instances for parallel processing
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G
  # ============================================================================
  # IMAGE ANALYSIS WORKER (NEW)
  # ============================================================================
  image-worker:
    build:
      context: .
      dockerfile: workers/image_worker/Dockerfile
    environment:
      # CRITICAL: These must match the mounted volume path
      HOME: /app/models/image
      PADDLEX_HOME: /app/models/image
      MODEL_BASE_DIR: /app/models/image
      JOB_LAUNCHER_URL: ${JOB_LAUNCHER_URL}
      JOB_SECRET_TOKEN: ${JOB_SECRET_TOKEN}
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_USER: ${NEO4J_USER}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
    volumes:
      # Mount shared media directory for accessing images
      - ${MEDIA_PATH}:/shared/media:ro
      # Mount shared library
      - ./aether_lib:/app/aether_lib:ro
      - image_models:/app/models/image
    networks:
      - aether-network
    depends_on:
      keycloak:
        condition: service_started
      image-models:
        condition: service_started
    restart: unless-stopped
    mem_limit: 4g
    mem_reservation: 2g
    healthcheck:
      test: ["CMD", "rq", "info", "--url", "redis://redis:6379/2"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # ===========================================================================
  # AUDIO TRANSCRIPTION WORKER
  # ============================================================================
  audio-worker:
    build:
      context: .
      dockerfile: workers/audio_worker/Dockerfile
    environment:
      JOB_LAUNCHER_URL: ${JOB_LAUNCHER_URL}
      JOB_SECRET_TOKEN: ${JOB_SECRET_TOKEN}
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_USER: ${NEO4J_USER}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
      MODELS_PATH: /app/models
      USE_QUANTIZATION: "true"
    volumes:
      - ./aether_lib:/app/aether_lib:ro
      - audio_models:/app/models/audio
      - ${MEDIA_PATH}:/shared/media:ro
    networks:
      - aether-network
    depends_on:
      keycloak:
        condition: service_started
      audio-models:
        condition: service_started
    restart: unless-stopped
    mem_limit: 6g
    deploy:
      resources:
        limits:
          memory: 6G
        reservations:
          memory: 1G

  emotion-worker:
    build:
      context: .
      dockerfile: workers/emotion_worker/Dockerfile
    environment:
      JOB_LAUNCHER_URL: ${JOB_LAUNCHER_URL}
      JOB_SECRET_TOKEN: ${JOB_SECRET_TOKEN}
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_USER: ${NEO4J_USER}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
      MODELS_PATH: /app/models
      USE_QUANTIZATION: "true"
    volumes:
      - ./aether_lib:/app/aether_lib:ro
      - emotion_models:/app/models/emotion
    networks:
      - aether-network
    depends_on:
      keycloak:
        condition: service_started
      emotion-models:
        condition: service_started
    restart: unless-stopped
    mem_limit: 4g
    deploy:
      replicas: 1
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 1G

  # ============================================================================
  # Classification WORKER
  # ============================================================================
  classification-worker:
    build:
      context: .
      dockerfile: workers/classification_worker/Dockerfile
    environment:
      JOB_LAUNCHER_URL: ${JOB_LAUNCHER_URL}
      JOB_SECRET_TOKEN: ${JOB_SECRET_TOKEN}
      NEO4J_URI: ${NEO4J_URI}
      NEO4J_USER: ${NEO4J_USER}
      NEO4J_PASSWORD: ${NEO4J_PASSWORD}
      MODELS_PATH: /app/models
      USE_QUANTIZATION: "true"
    volumes:
      - ./aether_lib:/app/aether_lib:ro
      - classifier_models:/app/models/classifier
      - ${MEDIA_PATH}:/shared/media:ro
    networks:
      - aether-network
    depends_on:
      keycloak:
        condition: service_started
      neo4j:
        condition: service_started
      classifier-models:
        condition: service_started
    restart: unless-stopped
    mem_limit: 3G
    deploy:
      resources:
        limits:
          memory: 3G
        reservations:
          memory: 1G

  # Add to docker-compose.yml
  geolocation-worker:
    build:
      context: .
      dockerfile: workers/geolocation_worker/Dockerfile
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - NEO4J_URI=${NEO4J_URI}
      - NEO4J_USER=${NEO4J_USER}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD}
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_PORT=${POSTGRES_PORT}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - JOB_LAUNCHER_URL=${JOB_LAUNCHER_URL}
      - JOB_SECRET_TOKEN=${JOB_SECRET_TOKEN}
      - PHOTON_URL=http://photon:2322
    volumes:
      - ./aether_lib:/app/aether_lib:ro
      - geolocation_models:/app/models/geolocation  # Cache for libpostal data
    networks:
      - aether-network
    depends_on:
      keycloak:
        condition: service_started
      geolocation-models:
        condition: service_started
    restart: unless-stopped
    deploy:
      replicas: 1  # Parallel processing
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M
  photon:
    build:
      context: .
      dockerfile: docker/photon/Dockerfile
    container_name: aether-photon
    ports:
      - "2322:2322"
    volumes:
      - ./models/geocoding/photon/photon_data:/photon/photon_data
    environment:
      - JAVA_OPTS=-Xmx2g
    networks:
      - aether-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:2322/api?q=Berlin"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    depends_on:
      - postgres

  translation-models:
    build:
      context: ./models
      dockerfile: translation/Dockerfile
    volumes:
      - translation_models:/models

  audio-models:
    build:
      context: ./models
      dockerfile: audio/Dockerfile
    volumes:
      - audio_models:/models

  emotion-models:
    build:
      context: ./models
      dockerfile: emotion/Dockerfile
    volumes:
      - emotion_models:/models

  classifier-models:
    build:
      context: ./models
      dockerfile: classifier/Dockerfile
    volumes:
      - classifier_models:/models

  image-models:
    build:
      context: ./models
      dockerfile: image/Dockerfile
    volumes:
      - image_models:/models

  geolocation-models:
    build:
      context: ./models
      dockerfile: geolocation/Dockerfile
    volumes:
      - geolocation_models:/models
  
volumes:
  pgdata:
  neo4j_data:
  neo4j_logs:
  keycloak_data:
  tg_sessions:
  redis-data:
  translation_models:
  audio_models:
  emotion_models:
  classifier_models:
  image_models:
  geolocation_models:

# ============================================================================
# NETWORKS
# ============================================================================

networks:
  aether-network:
    driver: bridge