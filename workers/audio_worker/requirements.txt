# Core runtime
redis==5.0.1
rq==1.15.1
requests==2.31.0
pydantic
# Audio transcription (ARM + CPU-safe versions)
# CPU-only PyTorch (installed from official PyTorch index via Dockerfile)
torch==2.2.2
openai-whisper==20240927
soundfile==0.12.1
numpy<2

# Language detection & utils
langdetect==1.0.9
Pillow==10.1.0
python-json-logger==2.0.7

# Optional: testing utilities (not used in production)
pytest==7.4.3
pytest-asyncio==0.21.1

# Neo4j client (shared library)
neo4j>=5.14.0